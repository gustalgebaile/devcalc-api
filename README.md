# devcalc-api

API REST em Java para operações matemáticas simples usando Javalin.

## Build e execução

Construído com Maven.

Para rodar localmente:

mvn clean compile exec:java -Dexec.mainClass="com.devcalc.App"